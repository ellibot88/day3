//ternary operator with single-line arrow function

const func = (number) => (number % 2 === 0 ? "True" : "False");

console.log(func(4));

//single-line arrow function

const myFunction = (param1, param2) => param1 + param2;
